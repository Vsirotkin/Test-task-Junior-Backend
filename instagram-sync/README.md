
# Instagram Sync Service

Тестовое задание для Junior Backend Developer (Python/Django).

## 📋 Описание

Сервис синхронизирует контент из Instagram Graph API в локальную базу данных и позволяет управлять комментариями через REST API.

## 🛠 Стек

- Python 3.13
- Django 6.0
- Django REST Framework
- PostgreSQL 18
- Docker & Docker Compose
- uv (менеджер зависимостей)
- pytest + requests-mock (тестирование)

## 🚀 Быстрый старт

### Вариант 1: Локальная разработка

```bash
# 1. Клонируй репозиторий
git clone <твой-repo-url>
cd instagram-sync

# 2. Скопируй переменные окружения
cp .env.example .env

# 3. Отредактируй .env и вставь свои значения:
# - SECRET_KEY (сгенерируй через: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())")
# - INSTAGRAM_ACCESS_TOKEN (получи в https://developers.facebook.com/apps/)
# - INSTAGRAM_USER_ID (твой Instagram User ID)

# 4. Запусти базу данных
make db-up

# 5. Установи зависимости
make install

# 6. Примени миграции
make migrate

# 7. Запусти сервер
make run
```

Сервер доступен по адресу: <http://127.0.0.1:8000/>

### Вариант 2: Docker

```bash
# 1. Скопируй переменные окружения
cp .env.example .env

# 2. Отредактируй .env (см. выше)

# 3. Запусти всё одной командой
make docker-up
```

Сервер доступен по адресу: <http://127.0.0.1:8000/>

## 📡 API Endpoints

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| `POST` | `/api/sync/` | Запуск синхронизации постов из Instagram |
| `GET` | `/api/posts/` | Список постов (с пагинацией CursorPagination) |
| `POST` | `/api/posts/{id}/comment/` | Создать комментарий к посту |

### Примеры запросов

#### Синхронизация постов

```bash
curl -X POST http://127.0.0.1:8000/api/sync/
```

#### Получить список постов

```bash
curl http://127.0.0.1:8000/api/posts/
```

#### Создать комментарий

```bash
curl -X POST http://127.0.0.1:8000/api/posts/1/comment/ \
  -H "Content-Type: application/json" \
  -d '{"text": "Great post!", "username": "testuser"}'
```

## 🧪 Тестирование

```bash
# Запустить все тесты
make test

# Запустить тесты в Docker
make docker-test
```

Тесты используют мокирование (`requests-mock`) и не выполняют реальные HTTP-запросы к Instagram API.

## 📁 Структура проекта

```
instagram-sync/
├── apps/
│   ├── posts/          # Модели, сериализаторы, views для постов и комментариев
│   └── sync/           # Логика синхронизации с Instagram
├── config/             # Настройки Django
├── compose.yml         # Docker Compose конфигурация
├── Dockerfile          # Образ приложения
├── Makefile            # Команды для разработки
├── pyproject.toml      # Зависимости проекта
└── README.md           # Этот файл
```

## 🔑 Архитектурные решения

1. **Разделение ответственности**: Логика работы с Instagram API вынесена в `services.py`, views остаются тонкими.
2. **Upsert логика**: При синхронизации используется `update_or_create` для обновления существующих постов.
3. **Пагинация**: CursorPagination для эффективной работы с большими объёмами данных.
4. **Тестирование**: Интеграционные тесты с мокированием внешних API-запросов.
5. **Типизация**: Type hints для ключевых методов и функций.

## 📝 Получение Instagram Access Token

1. Создай приложение в [Facebook App Dashboard](https://developers.facebook.com/apps/)
2. Добавь продукт "Instagram Graph API"
3. Получи временный токен в [Graph API Explorer](https://developers.facebook.com/tools/explorer/)
4. Скопируй токен в `.env` файл

## ⚠️ Примечания

- Для локальной разработки используется PostgreSQL в Docker-контейнере
- Тесты не выполняют реальные запросы к Instagram API (используется requests-mock)
- Проект готов к расширению функциональности

## 👤 Автор

**VikSir**
